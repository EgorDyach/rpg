# 🎮 RPG Backend - Игровая система саморазвития для студентов

Django REST API для геймификации саморазвития студентов с системой квестов, достижений, рейтингов и социального взаимодействия.

## 🚀 Основные возможности

### 1. Авторизация и роли

- Регистрация по email/логину
- Роли: **Студент** и **Администратор**
- JWT аутентификация

### 2. Система квестов

- **Студенты создают свои квесты** (личные или публичные)
- Публичные квесты доступны всем для принятия
- Автоматическое начисление XP и монет при выполнении
- Бонус за выполнение раньше дедлайна (+20% XP)

### 3. Прокачка персонажа

- Система уровней с автоматическим повышением
- Опыт (XP) и монеты
- Streak (серия дней без пропуска)
- Статистика выполнения квестов

### 4. Таблица лидеров

- Рейтинги по уровню, XP, количеству квестов, streak
- Фильтры по периоду (все время, неделя, месяц)
- Фильтры по факультету и группе

### 5. Социальное взаимодействие

- Комментарии к публичным квестам
- Лайки выполненных квестов
- Группы (гильдии) с постами и комментариями
- Групповые цели

### 6. Система достижений

- Автоматическая проверка достижений:
  - 5 выполненных квестов подряд
  - 7-дневный streak
  - Создание 10 квестов
  - Выполнение цели раньше дедлайна
  - Активность в комментариях
  - Участие в групповых челленджах

### 7. Уведомления

- О новых публичных квестах
- О выполнении квестов
- О получении достижений
- О повышении уровня
- О комментариях и лайках
- О приглашениях в группы

## 📚 API Документация (Swagger)

После запуска сервера доступна документация:

- **Swagger UI**: http://localhost:8000/api/docs/
- **ReDoc**: http://localhost:8000/api/redoc/
- **Schema JSON**: http://localhost:8000/api/schema/

## 🛠 Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Миграции

```bash
python manage.py makemigrations
python manage.py migrate
```

### 3. Генерация тестовых данных (опционально)

Для быстрого тестирования можно сгенерировать моковые данные:

```bash
# Базовый вариант (20 пользователей, 50 квестов, 5 групп)
python manage.py generate_mock_data

# С параметрами
python manage.py generate_mock_data --users 50 --quests 100 --groups 10

# Очистить существующие данные и создать новые
python manage.py generate_mock_data --clear
```

Скрипт создаст:

- Пользователей (студентов) с разными уровнями и XP
- Квесты (личные и публичные)
- Группы с участниками
- Назначения квестов (часть выполненных)
- Комментарии и лайки
- Посты в группах
- Групповые цели
- Достижения

### 4. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

### 5. Запуск сервера

```bash
python manage.py runserver
```

## 📡 Основные эндпоинты

### Аутентификация

- `POST /api/users/` - Регистрация
- `POST /api/token/` - Получение JWT токена
- `POST /api/token/refresh/` - Обновление токена
- `GET /api/users/me/` - Текущий пользователь
- `GET /api/users/stats/` - Статистика пользователя

### Квесты

- `GET /api/quests/` - Список квестов (публичные + свои)
- `POST /api/quests/` - Создать квест (требует авторизации)
- `POST /api/quests/{id}/accept/` - Принять публичный квест
- `GET /api/assignments/` - Мои назначенные квесты
- `POST /api/assignments/{id}/complete/` - Выполнить квест

### Группы

- `GET /api/groups/` - Список групп
- `POST /api/groups/` - Создать группу
- `POST /api/groups/{id}/join/` - Присоединиться к группе
- `POST /api/groups/{id}/leave/` - Покинуть группу
- `GET /api/group-posts/?group={id}` - Посты группы
- `POST /api/group-posts/` - Создать пост в группе

### Комментарии и лайки

- `GET /api/quest-comments/?quest={id}` - Комментарии к квесту
- `POST /api/quest-comments/` - Добавить комментарий
- `POST /api/quest-likes/` - Лайкнуть выполненный квест

### Рейтинги

- `GET /api/leaderboard/rankings/?period=week&sort_by=level` - Рейтинг
  - `period`: `all`, `week`, `month`
  - `sort_by`: `level`, `xp`, `quests`, `streak`

### Достижения

- `GET /api/achievements/` - Список всех достижений
- `GET /api/achievement-progress/` - Прогресс по достижениям

### Уведомления

- `GET /api/notifications/` - Мои уведомления
- `PATCH /api/notifications/{id}/` - Отметить как прочитанное

## 🔧 Технологии

- **Django 4.2+**
- **Django REST Framework**
- **JWT (Simple JWT)**
- **drf-spectacular** (Swagger/OpenAPI)
- **PostgreSQL** (опционально, по умолчанию SQLite)

## 📝 Структура проекта

```
rpg-backend/
├── api/
│   ├── models.py          # Модели данных
│   ├── views.py           # ViewSets и эндпоинты
│   ├── serializers.py     # Сериализаторы
│   ├── permissions.py     # Права доступа
│   ├── utils.py           # Утилиты (XP, уровни, достижения)
│   ├── urls.py            # Маршруты API
│   ├── admin.py           # Админка Django
│   └── signals.py         # Сигналы (создание профиля)
├── dekancraft/
│   ├── settings.py        # Настройки Django
│   └── urls.py           # Главные URL
├── requirements.txt
└── manage.py
```

## 🎯 Примеры использования

### Регистрация и авторизация

```bash
# Регистрация
curl -X POST http://localhost:8000/api/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "student1",
    "email": "student1@example.com",
    "password": "password123",
    "role": "student",
    "faculty": "ИТ",
    "group_name": "ИВТ-21"
  }'

# Получение токена
curl -X POST http://localhost:8000/api/token/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "student1",
    "password": "password123"
  }'
```

### Создание квеста

```bash
curl -X POST http://localhost:8000/api/quests/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Изучить Django REST Framework",
    "description": "Пройти курс по DRF",
    "goal": "Создать API для проекта",
    "is_public": true,
    "xp_reward": 100,
    "coin_reward": 50,
    "deadline": "2024-12-31T23:59:59Z",
    "difficulty": 3
  }'
```

### Выполнение квеста

```bash
curl -X POST http://localhost:8000/api/assignments/1/complete/ \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🔐 Права доступа

- **Студенты** могут:
  - Создавать квесты (личные и публичные)
  - Принимать публичные квесты
  - Выполнять свои квесты
  - Комментировать и лайкать
  - Создавать группы и посты
  - Просматривать рейтинги

- **Администраторы** могут:
  - Все, что могут студенты
  - Управлять пользователями
  - Управлять курсами и группами
  - Создавать достижения
  - Просматривать все данные

## 📊 Система уровней

Формула расчета XP для уровня: `100 * (уровень ^ 1.5)`

При достижении необходимого XP уровень автоматически повышается, остаток XP сохраняется.

## 🏆 Достижения

Достижения проверяются автоматически при:

- Выполнении квестов
- Обновлении streak
- Создании квестов
- Комментировании
- Участии в групповых целях

## 📝 Примечания

- Все даты и время в UTC
- JWT токены действительны 60 минут (access) и 7 дней (refresh)
- Streak обновляется при любой активности пользователя
- Уведомления создаются автоматически при различных событиях

## 🐳 Docker (опционально)

Для использования PostgreSQL через Docker:

```bash
docker-compose up -d
python manage.py migrate
```

## 📄 Лицензия

MIT
